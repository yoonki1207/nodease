# Nodease Documentation

Status: Untrusted
Authority: None
Source of Truth: No

> **Warning**
> The entire `docs/` directory is currently untrusted. Do not use any file in
> this directory as the source of truth for product requirements, architecture,
> APIs, data models, security policy, testing, or release decisions.

## Trust Boundary

Existing `Active`, `Accepted`, and `Verified Against` labels are historical
claims and do not establish current authority.

Determine current behavior from the checked-out code, executable tests,
database migrations, and deployment configuration. Product intent must be
decided explicitly; do not infer new requirements from deleted or retained
historical documents.

## Retained Material

Files temporarily retained under `demo/` and `learning/` are operational or
educational references only. They are not product specifications or release
evidence.

## Revalidation

No document becomes authoritative automatically. A future document must be
explicitly reviewed against one selected code revision and separately approved
before it can be declared a source of truth.
